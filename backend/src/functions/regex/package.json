{
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.x",
    "@aws-sdk/lib-dynamodb": "^3.x",
    "zod": "^3.x"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.120"
  },
  "name": "regex",
  "description": "This is a package meant to filter for packages matching the regex given.",
  "version": "1.0.0",
  "main": "regex.js"
}
